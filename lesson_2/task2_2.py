# Manipulate strings
# Save your first and last name as separate variables, then
# use string concatenation to add them together with a white space in between and print a greeting.
first_name = 'Oleg'
last_name = 'Seniuta'
solv = first_name +' '+ last_name
print(f'Welcome! {solv}')